# David 重評 — LIVE pilot packet (2026-09-22)

**Date:** 2026-09-22 07:16 CST (Asia/Taipei)  
**Tree:** `/workspace/remember-me` @ `d8a7548` (local, unpushed)  
**Against:** `SCORECARD.md`, `docs/reviews/LIVE_PILOT_2026-09-22.md`, `LIVE_SMOKE_2026-09-22.json`, `bakeoff_metrics_live.json`  
**Method:** Read pilot + smoke JSON + live metrics; spot policy `T_ACCEPT=0.85` / `T_ESCALATE=0.55`; `.venv/bin/pytest -q` → **140** dots green (offline).

---

## Verdict

**`APPROVE_WITH_CONDITIONS`** — keep PREVIEW / Pre-Skill. Live **contract** proved; quality / acceleration **falsified**.  
**`REJECT` ≥9.5** / formal skill / “Jev accelerates memory” on this evidence.

**Weighted overall: ~8.5** (aligns with author ~8.5; Goal fit cut vs prior David 9.0).

---

## Leon 10-pt (re-score)

| Dimension | Weight | Prior David | Now | Notes |
|-----------|-------:|------------:|----:|-------|
| Evidence | 20% | 7.5 | **8.0** | Live smoke + `bakeoff_metrics_live.json` real; precision Δ **−0.387** (honest fail) |
| Goal fit | 20% | 9.0 | **8.5** | Dual-gate architecture still fits; live useful-gate half **not** met under hash-only |
| Runtime | 15% | 8.0 | **8.5** | Pin `jev-1.13.0`; ~465 ms smoke; emit OK; criteria shapes fixed |
| Verification | 15% | 8.5 | **8.5** | Offline chaos + live 200s; **remote CI on main not claimed** |
| Safety | 10% | 9.0 | **9.0** | Default stays redacted; do **not** lower bands to match 0.2–0.5 noise |
| License | 10% | 9.0 | **9.0** | unchanged |
| Maintainability | 10% | 8.0 | **8.0** | Push blocked (PAT lacks `workflow`); local green ≠ remote green |
| **Weighted** | 100% | ~8.4 | **~8.5** | |

```text
0.20×8.0 + 0.20×8.5 + 0.15×8.5 + 0.15×8.5 + 0.10×9.0 + 0.10×9.0 + 0.10×8.0
= 1.60 + 1.70 + 1.275 + 1.275 + 0.90 + 0.90 + 0.80
= 8.45 ≈ **8.5**
```

---

## What live proves / falsifies

**Proves:** HttpJev System One contract (Choice dict / Score string levels); pin echo; fail_closed_rate 0.0 on n=50; smoke malformed=false / deny=false.

**Falsifies:** Acceleration / quality win under current thresholds + hash-only state (precision 0.387→0.0, p95 +~509 ms). Collapse is **threshold skip**, not HTTP failure.

---

## Threshold / state enrichment path (David gate)

| Path | Allowed? | Condition |
|------|----------|-----------|
| Structured enrichment **without** raw query (intent class, length bucket, candidate stub tags, clearer criteria text) | **YES** | Prefer this; pre-register bake-off bars; track escalate rate |
| Recalibrate `T_ACCEPT` / `T_ESCALATE` **after** enrichment | **YES** | Held-out calibrate; no chasing live conf noise |
| Blind lower thresholds to admit conf≈0.2–0.5 | **NO** | Admits noise; Safety regression |
| `include_raw_query` / free-text preview as bake-off default | **NO** | Opt-in only + formal risk accept + independent human redact + GateAudit always on |
| Re-run bake-off before enrichment / pre-reg bars | **NO** | Wastes budget; theater |

---

## Remaining ＜9.5 blockers

1. Useful live decisions under redaction (enrich first → then calibrate)  
2. Live bake-off meeting **pre-registered** quality bars (today: FAIL)  
3. Remote CI green on `main` (fix PAT `workflow` scope / proof)  
4. Independent **human** redaction review (esp. before any preview/raw expansion)  
5. Leon formal sign-off  

Hard deal-breakers unchanged: no ≥9.5 self-claim; no FakeJev-as-live; no acceleration narrative.
