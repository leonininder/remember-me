# David review — remember-me (2026-09-20)

**Verdict:** `APPROVE_WITH_CONDITIONS` — internal Pre-Skill / PREVIEW only.  
**REJECT** public showcase / investable narrative / any “Jev accelerates memory” claim.

Dual-signs Justin Sun’s conditions; adds:

- No `.github/` on main / dead CI badge
- HttpJev contract mismatch (`/v1/jev` vs System One `/v1/systemone`) + default raw `query` egress
- `bakeoff_metrics.json` gitignored; SCORECARD p95 drift vs JSON
- No persistent GateAuditRecord

## Must-fix

**P0:** Strip accelerate/Hindsight marketing; fix or remove dead CI badge; freeze SCORECARD ≤8.5 honesty; Non-goal: no official agent plugins.  
**P1:** Fix HttpJev→System One; query/tags egress; optional gate audit.  
**P2:** Live personal-prefs pilot; dual-track bake-off; pyproject Homepage/PREVIEW classifiers.

## Leave PREVIEW only when

Live hydrate logs (pin `jev-1.13.0`) + live deltas + real chaos + independent redaction review + CI on main + Leon sign-off. Do not advertise overall ≥9.0 as proven before that.

Source: David bot adversarial gate, 2026-09-20.
