# memorybench_tfl

Open fixture pack for Temporal Fact Ledger (TFL) evals.

- `schema/ontology_v0.json` — Phase B seed ontology (PLAN §4.2.1)
- `schema/candidate_fact.schema.json` — CandidateFact closed schema (PLAN §4.2)
- `suites/` — labeled scenarios (Phase D expands)
- `labels/pairs.jsonl` — relation-labeled pairs (Phase D)

Phase B uses ontology + schema for FactKey minting and admit validation only.
